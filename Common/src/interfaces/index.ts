export interface ConfigurationInterface {
	login: string,
	id: number
}
export interface User {
	name: string,
	age: number,
	isActive: boolean
}

export interface OnSendInfoInterface {
	timeRemaining: number;
}
